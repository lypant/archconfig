1. Download the suite
curl -L https://github.com/lypant/archconfig/tarball/simplified | tar xz

T.B.C.

TODO LIST:
- When copying log file after install - not complete log file is being copied, since after copy operation few logs are added. Think about better solution.

# To set custom video mode for terminal (available under vga=864 or vesa 360) run in host's terminal VBoxManage setextradata "<guest_name>" "CustomVideoMode1" "1680x1050x24"

